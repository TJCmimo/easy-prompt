# 任务类型配方：先分类，再抓药

> 源：PDF 第 5 章（配方组合、先发散再收敛、先推理后结论、隐式语义）
> 判别不清时不要硬猜——这是关键缺口，反问用户（见 five-elements.md）。

## 类型判别速查

| 类型 | 判别特征 |
| --- | --- |
| 学习理解类 | 解释概念/代码/报错，"X 是什么""这段在干嘛" |
| 工程执行类 | debug、改码、修构建、跑通某功能 |
| 决策比较类 | 选项已存在：A/B/C 选哪个、要不要重构 |
| 猜想探索类 | 选项不存在的开放问题："怎么设计""有什么创新点""还有什么可能" |
| 内容生成类 | 写文档、注释、README、报告、脚本 |

## 学习理解类

- 判别特征：解释概念/代码/报错，"X 是什么""这段在干嘛"。
- 催化剂配方：先讲直觉 + 最小例子 + 检查理解（解释类为主）。
- 默认限制（缺失时标【推断】）：不要一开始堆专业术语；不要假设我已懂 <相关前置概念>。
- 默认验收（缺失时标【推断】）：我能复述核心思想 + 出一道判断题检查理解。
- 转写模板：

```text
目标：用适合初学者的方式解释 <对象>，先整体作用再分模块
背景：【推断】RM <方向> 组新生，<语言/工具> 基础一般
输入：<粘贴代码/概念名/报错>
限制：【推断】每个术语第一次出现时解释；每次只引入一个新概念 ← 可改
处理方法：先讲直觉再讲定义；用一个最小例子
验收：【推断】最后出一道题检查我是否真正理解 ← 标准可改
```

## 工程执行类

- 判别特征：debug、改码、修构建、跑通某功能。
- 催化剂配方：先拆解原因 + 最小修改 + 测试验证（拆解类 + 执行类 + 验证类各取一）。
- 默认限制（缺失时标【推断】）：先不要改代码，只分析原因；不要给危险命令（删文件/重装环境）；不要修改无关文件。
- 默认验收（缺失时标【推断】）：构建通过/原有测试全过 + 说明修改内容与回滚方法。
- 转写模板：

```text
目标：定位并修复 <问题>，优先最小修改
背景：【推断】正在使用的 RM 项目，不能大范围重构
输入：<粘贴报错/日志/相关代码>
限制：【推断】先列原因再动手；不要删除依赖；不要改无关文件 ← 可改
处理方法：先列可能原因按概率排序，选最小修改方案；改后运行测试
验收：【推断】<构建命令> 通过；说明改了什么、怎么回滚；需要我补什么信息就直接问 ← 标准可改
```

## 决策比较类

- 判别特征：选项已存在：A/B/C 选哪个、要不要重构。
- 催化剂配方：统一比较维度 + 最强反方观点 + 最后推荐（严谨类为主）。
- 要点：先推理后结论——先给结论 AI 会变辩护律师，先分析才像评审专家。
- 默认限制（缺失时标【推断】）：先完成全部比较再给推荐；不要顺着我已有的倾向圆。
- 默认验收（缺失时标【推断】）：每个选项在同一组维度下有分析 + 明确推荐 + 推荐的适用边界。
- 转写模板：

```text
目标：比较 <A/B/C>，先不下结论
背景：【推断】RM 队内项目，维护者以新生为主
输入：<各选项的现状/材料>
限制：【推断】先完成全部比较再给推荐；不要顺着我已有的倾向圆 ← 可改
处理方法：从成本、难度、风险、长期维护四个维度统一比较；对推荐项给最强反方观点
验收：【推断】比较表 + 推荐 + 什么情况下应改选其他项 ← 标准可改
```

## 猜想探索类（两段式输出）

- 判别特征：选项不存在的开放问题："怎么设计""有什么创新点""还有什么可能"。
- 催化剂配方：先发散（禁止过早给最终方案）→ 再收敛（按明确标准筛选）。
- 默认限制（缺失时标【推断】）：发散阶段不要收敛、不要评分排序；每个方向给一段核心思路即可。
- 默认验收（缺失时标【推断】）：发散阶段至少 4 个方向，每个含核心思路与主要代价；收敛阶段给出最推荐的 1-2 个方案、推荐理由、最强反方观点，以及第一步行动。
- 转写模板：产出下面两条提示词，等 AI 展开完再发第二条；鼓励用户在两条之间补充自己的筛选偏好。

第一条（发散）模板：

```text
目标：探索 <问题> 的可能方向，先不要给最终方案
背景：【推断】RM 队内项目，人手与经费有限
输入：<现状/已有条件>
限制：【推断】现在不要收敛、不要评分排序；每个方向给一段核心思路即可 ← 可改
处理方法：从常规方案、低成本方案、激进方案、长期方案四个角度展开【推断-角度可换】
验收：【推断】至少 4 个方向，每个含核心思路与主要代价 ← 标准可改
```

第二条（收敛）模板：

```text
现在收敛。根据实现难度、成本、稳定性、适合新手程度筛选【推断-标准可换】，
给出最推荐的 1-2 个方案、推荐理由、最强反方观点，以及第一步行动。
```

（补充我的筛选偏好：______）

## 内容生成类

- 判别特征：写文档、注释、README、报告、脚本。
- 催化剂配方：格式例子（隐式语义）+ 明确验收。
- 要点：一个高质量例子比一大段格式描述更有效；例子会把 AI 带进正确状态。
- 默认限制（缺失时标【推断】）：格式仿照下面的例子；不要超过 <长度>。
- 默认验收（缺失时标【推断】）：格式与例子一致 + 覆盖 <必须包含的内容点>。
- 转写模板：

```text
目标：写 <文档/注释/脚本>
背景：【推断】读者是 RM 新队员
输入：<素材/代码/要点>
限制：【推断】格式仿照下面的例子；不要超过 <长度> ← 格式可改
处理方法：先列大纲让我确认，再写全文
验收：【推断】包含 <要点清单>；风格与例子一致 ← 要点可改
例子：<贴一段目标风格的样例>
```

## 通用纪律（各类型都适用）

- 不写死步骤：给目标和边界，不给"必须先 ls 再 cat"——写死会把 AI
  困在使用者的认知上限，失去寻优能力。
- 环境适配：目标是 coding agent（会改文件跑命令）→ 强调最小修改/测试/回滚；
  目标是聊天 AI → 强调理解难度控制与输出结构。
